#urls 
NEXT_PUBLIC_WS_URL=http://localhost:4000
NEXT_PUBLIC_CAM_URL="https://2zs77txm-5000.asse.devtunnels.ms/video_feed"
AI_BASE_URL=http://localhost:8000
NEXT_PUBLIC_ASSISTANT_URL="http://localhost:5055"
TELEMETRY_BASE_URL=http://localhost:4000
OLLAMA_URL="http://localhost:11434"
NEXT_PUBLIC_VERCEL_URL="fishlinic.vercel.app"
VERCEL_URL="fishlinic.vercel.app"

#ai related
ASSISTANT_MODEL="qwen2.5:3b"

#node environment
NODE_ENV=production

#cors
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://fishlinic.vercel.app

# auth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="iQkVRMmGbeEPrGdnjzIyr38/2dy70ZkAsw3JqpsNm0I="
### auth-providers
GOOGLE_CLIENT_ID="..."
GOOGLE_CLIENT_SECRET="......"
KAKAO_CLIENT_ID=".."
KAKAO_CLIENT_SECRET="..."

# This was inserted by `prisma init`:
# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres 
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the 
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

#db
DATABASE_URL=""